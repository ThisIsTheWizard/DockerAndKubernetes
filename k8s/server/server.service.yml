apiVersion: v1
kind: Service
metadata:
  name: docker-and-kubernetes-server-service
spec:
  selector:
    app: docker-and-kubernetes-server
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000